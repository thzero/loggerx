name: loggerx
description: Tiny but powerful logger with default root logger and ability to create custom loggers with runtime level filtering.
version: 2.0.0
repository: https://github.com/thzero/loggerx

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  intl: ^0.20.2
dev_dependencies:
  lints: ^6.0.0